// programa tabuada

for (int t = 1; t <= 10; t++)
{
    for (int contador = 1; contador <= 10; contador++)
    {
        int formula = t * contador;
        Console.WriteLine(t + " X " + contador + " = " + formula);
    }
}